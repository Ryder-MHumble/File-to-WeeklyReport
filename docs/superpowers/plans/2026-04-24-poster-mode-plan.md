# 海报模式实施计划

日期：2026-04-24

## 1. 实施拆分

1. 顶层模式切换
   - 拆分 `App.jsx`
   - 新增统一头部和模式切换入口

2. 周报工作台隔离
   - 将原有周报工作区收敛到 `ReportWorkbench.jsx`
   - 保持原控制器不变

3. 海报工作台落地
   - 新增海报控制区、海报预览区、海报控制器
   - 接入多文档输入与手工文本输入

4. OpenRouter 海报链路
   - 新增 brief prompt
   - 新增 image prompt
   - 接入 `OPENROUTER_POSTER_BRIEF_MODEL`
   - 接入 `OPENROUTER_POSTER_IMAGE_MODEL`

5. 回退与日志
   - 本地 SVG 草图回退
   - 补齐业务 JSON 与系统日志

6. 文档与验收
   - 更新 `README.md`
   - 更新 `.env.example`
   - 执行真实集成验证并记录证据

## 2. Ralph Loop 验证点

### 增量 1

- 内容：完成模式切换和工作台拆分
- 验证：页面可切换两种模式，周报默认渲染正常

### 增量 2

- 内容：完成海报控制区与本地回退草图
- 验证：海报模式可输入文本并生成草图

### 增量 3

- 内容：接入真实 OpenRouter brief + 图片模型
- 验证：海报模式可返回真实图片并支持下载

### 增量 4

- 内容：回归周报模式并补文档
- 验证：周报生成主链路仍可用，构建通过

## 3. 风险点

- 图片模型返回结构与文本模型不同，前端解析可能失败
- 中文海报排版不完全可控，需要保留回退链路
- Vite 开发服务默认没有代理，需要显式指向 `server/app.js`

## 4. 当前结论

- 已完成海报模式独立分支实现
- 已完成周报模式回归
- 已完成 OpenRouter 真实海报调用与下载验证
